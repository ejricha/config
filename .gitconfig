# ~/.gitconfig
# 
# Configuration file for git

[color]
	ui = true
[color "branch"]
	current = yellow reverse
	local = yellow
	remote = green
[color "diff"]
	meta = yellow bold
	frag = magenta bold
	old = red bold
	new = green bold
[color "status"]
	added = yellow bold
	changed = green bold
	untracked = cyan bold
[core]
	whitespace=fix,-indent-with-non-tab,trailing-space,cr-at-eol
	excludesfile = ~/.gitignore-rab
	editor = nvim
	pager = cat
[stash]
	pager = less
[apply]
	whitespace = nowarn
[branch]
	autosetuprebase = always
[rerere]
	enabled = 1
[log]
	date = format:%Y-%m-%d %H:%M
[format]
	pretty = tformat:%C(yellow bold)%h  %C(cyan)%ad  %C(green)%<(15,trunc)%an%Creset  %s%C(magenta)%d%Creset
[alias]
	################
	a = add
	amend = !git add -A && git commit --amend --no-edit # Add to the previous commit
	################
	br = branch
	################
	ci = commit
	commi = commit
	commt = commit
	commti = commit
	m = !git commit -m
	################
	co = checkout
	################
	current = "!git rev-parse --abbrev-ref --symbolic-full-name @{u}"
	################
	d = diff --color-words='[^[:space:]]'
	df = diff --color-words='[^[:space:]]'
	diffunpushed = diff @{u}..
	################
	gr = log --graph
	graph = log --graph --oneline --decorate=short --branches='*'
	lol = log --graph --color=always
	################
	l = log
	lg = log --name-status --color=always
	logfollow = log --follow
	################
	lgg = "!git logunpushed ${@} && git logpushed -5 ${@}"
	logformat = "!git log --pretty=\"tformat:%C(yellow bold)%h  %C(cyan)%ad  %C(green)%<(15,trunc)%an  %Creset${TEXT}%s%C(magenta)%d%Creset\""
	logpushed = "!C=$(git current) && TEXT=\"%C(white)\" git logformat ${C}"
	logunpushed = "!C=$(git current) && TEXT=\"%C(cyan bold)\" git logformat ${C}..HEAD"
	################
	patch = am --reject --ignore-whitespace -i # Apply patch, ignoring whitespace
	################
	pend = cherry -v origin/master # Show pending changes to master branch
	################
	r = remote -v
	################
	reorder = "!GIT_SEQUENCE_EDITOR=\"sed -i -n 'h;1n;2p;g;p'\" git rebase -i HEAD~2" # Reorder the last two commits
	################
	s = status
	stats = status
	statsu = status
	statu = status
	st = status -sb
	################
	sh = show --color=always
	################
	up = pull
	################
	who = "!f() { W=$1; shift; git log --author=$W $@; }; f"
	w = !git who
	################
[push]
	default = simple
[http]
	sslVerify = true
[user]
	email = ejricha+github@gmail.com
	name = Eric Richardson
[credential]
	helper = cache --timeout=3600
[include]
	path = ~/.gitconfig.custom
